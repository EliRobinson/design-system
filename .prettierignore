# Uses gitignore syntax — a leading `./` matches nothing.
node_modules
dist
.nx
.claude
apps/storybook/storybook-static
apps/docs/.next
apps/docs/next-env.d.ts
design-system-docs
pnpm-lock.yaml
apps/docs/public/brand
# Vendored verbatim from vercel/ai-elements by `pnpm sync:elements`, which
# re-derives every file from the pinned upstream release. Formatting them would
# make all 74 diverge from what that script expects, so the next re-sync would
# report the whole tree as locally edited and refuse to bump anything upstream
# had touched. `pnpm format` must leave this alone; the package's own scripts/
# and types/ are ours and are NOT ignored.
packages/ai-elements/src

# The Miltinson × AI Elements theme, landed verbatim from its design handoff.
# Same reasoning as the vendored tree above and one step weaker: these six files
# are authored elsewhere and are re-landed whole when the handoff moves, so a
# formatting pass here turns the next drop into a diff nobody can read. The
# imports that wire them are in apps/docs/src/app/elements.css, which IS ours
# and IS formatted.
apps/docs/src/app/ai-theme

# The design handoff bundle itself — reference material, not our source.
temp
