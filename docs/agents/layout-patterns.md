# Layout patterns

Header, Footer, Hero, Sidebar, TopBar, and StatCard are **app-specific layout compositions**. They are not published as `@elirobinson/react` exports.

Prototype them in `design-system-docs/ui_kits/` and compose in apps from `@elirobinson/react` primitives. See Storybook **Patterns/Marketing** for examples.
